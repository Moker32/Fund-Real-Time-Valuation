# 工作计划：在根目录创建 AGENTS.md 文档

## 目标
分析代码库并创建 AGENTS.md 文件，供 AI Agent 操作使用。

## 研究结果摘要

已收集的信息：
- **项目结构**: Python TUI 应用，基于 Textual 框架
- **依赖**: textual>=0.50.0, httpx>=0.24.0, akshare>=1.10.0, yfinance>=0.2.0
- **代码风格**: dataclass、Enum、ABC 抽象基类、中文注释、完整类型注解
- **测试**: pytest + pytest-asyncio，使用 unittest.mock
- **架构**: 三层架构（UI层、数据源层、配置层）

## 执行任务

**执行方式**: 使用 `/start-work` 执行此计划

**具体步骤**:

1. **创建 AGENTS.md 文件**
   - 路径: `/Users/huangxiang/work/Fund-Real-Time-Valuation/AGENTS.md`
   - 内容包含:
     - 项目概述
     - 常用命令（运行、测试）
     - 技术栈说明
     - 代码风格规范（导入顺序、数据模型、枚举、抽象类、异常处理、命名约定）
     - 架构概览（三层结构）
     - 测试规范（命名、Mock、pytest-asyncio）
     - 关键设计模式（数据源管理器、结果封装）
     - 配置存储位置
     - UI 快捷键
     - 开发注意事项

2. **文档验证**
   - 检查文件是否成功创建
   - 验证内容完整性（约 150 行）
   - 确保包含从 CLAUDE.md 和代码分析中提取的所有关键信息

## 验收标准

- [ ] AGENTS.md 文件存在于项目根目录
- [ ] 文件包含项目概述、命令、代码风格、架构等核心章节
- [ ] 文档长度约 150 行
- [ ] 内容准确反映代码库实际结构

## 注意事项

- 遵循 DRY 原则
- 保持代码风格规范与现有代码一致
- 确保文档清晰供 AI Agent 理解
